```C++
random(min, max); // Genera un número entre min (incluyendolo) y max (excluendolo) en formato long

randomSeed(valor); // Indica el primer número para el generador de señales aleatorias (random())
```