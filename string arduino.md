```c
String palabra;
palabra = palabra + `c`; //extiende el string 
palabra.length(); //devuelve la longitud de la variable
palabra.remove(posicion, numero); //se quitan los caracteres desde posción o el número indicado en número
```